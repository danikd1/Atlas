
  # RSS Reader

  This is a code bundle for RSS Reader. The original project is available at https://www.figma.com/design/4jYqnbz2WaoCeEUnhZenaJ/RSS-Reader.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  